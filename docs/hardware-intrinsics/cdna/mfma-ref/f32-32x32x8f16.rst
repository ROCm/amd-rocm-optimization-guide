.. RST include fragment -- do not add to toctree.
   Intrinsic: __builtin_amdgcn_mfma_f32_32x32x8f16
   Include via: .. include:: mfma-ref/f32-32x32x8f16.rst

``__builtin_amdgcn_mfma_f32_32x32x8f16``
"""""""""""""""""""""""""""""""""""""""""

.. code-block:: cpp

   v16float __builtin_amdgcn_mfma_f32_32x32x8f16(
       v4half   srcA,
       v4half   srcB,
       v16float srcC,
       int      cbsz,
       int      abid,
       int      blgp);

Computes one step of a :math:`32 \times 32` FP32 outer-product accumulation. 
Each instruction processes :math:`K=8` FP16 column elements of :math:`\pmb{A}`
and :math:`K=8` FP16 row elements of :math:`\pmb{B}`, accumulating into a
:math:`32 \times 32` FP32 tile held across 16 accVGPRs per lane. This intrinsic
operates on a single input block.

.. list-table::
   :header-rows: 1
   :widths: auto

   * - Parameter
     - Type
     - Description
   * - ``srcA``
     - v4half
     - Four elements of :math:`\pmb{A}` per lane.
   * - ``srcB``
     - v4half
     - Four elements of :math:`\pmb{B}` per lane.
   * - ``srcC``
     - v16float
     - Accumulator input: 16 FP32 elements per lane.
   * - ``cbsz``
     - int
     - Control Broadcast Size modifier, see :ref:`cdna-mfma-common-parameters`.
       Must be ``0``.
   * - ``abid``
     - int
     - :math:`\pmb{A}`-matrix Broadcast Identifier,
       see :ref:`cdna-mfma-common-parameters`.
       Must be ``0``.
   * - ``blgp``
     - int
     - :math:`\pmb{B}`-matrix Lane Group Pattern modifier, see
       :ref:`cdna-mfma-common-parameters`.

**Returns** ``v16float`` -- updated accumulator
(:math:`\text{srcA} \times \text{srcB} + \text{srcC}`).
