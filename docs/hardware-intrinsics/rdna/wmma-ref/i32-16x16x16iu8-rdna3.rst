.. RST include fragment -- do not add to toctree.
   Intrinsic: __builtin_amdgcn_wmma_i32_16x16x16_iu8_w32
   Include via: .. include:: wmma-ref/i32-16x16x16iu8-rdna3.rst

``__builtin_amdgcn_wmma_i32_16x16x16_iu8_w32``
""""""""""""""""""""""""""""""""""""""""""""""

Signature and parameters for this intrinsic.

.. code-block:: cpp

   v8int __builtin_amdgcn_wmma_i32_16x16x16_iu8_w32(
       bool  a_neg,
       v4int srcA,
       bool  b_neg,
       v4int srcB,
       v8int srcC,
       bool  clamp);

Computes one step of a dense :math:`16 \times 16` INT32 accumulation with
8-bit integer inputs.  Sixteen INT8 elements of :math:`\pmb{A}` are packed
into four ``int`` registers per lane; likewise for :math:`\pmb{B}`.

.. list-table::
   :header-rows: 1
   :widths: auto

   * - Parameter
     - Type
     - Description
   * - ``a_neg``
     - bool
     - ``true`` for signed INT8, ``false`` for unsigned UINT8.  Compile-time
       constant, see :ref:`rdna3-wmma-common-parameters`.
   * - ``srcA``
     - v4int
     - Sixteen 8-bit elements of :math:`\pmb{A}` per lane, packed into four
       32-bit registers (four 8-bit values per register).
   * - ``b_neg``
     - bool
     - ``true`` for signed INT8, ``false`` for unsigned UINT8 in
       :math:`\pmb{B}`.  Compile-time constant, see
       :ref:`rdna3-wmma-common-parameters`.
   * - ``srcB``
     - v4int
     - Sixteen 8-bit elements of :math:`\pmb{B}` per lane, packed into four
       32-bit registers.
   * - ``srcC``
     - v8int
     - Accumulator input: eight INT32 elements per lane.
   * - ``clamp``
     - bool
     - Clamp output to input type range on overflow.  Compile-time constant,
       see :ref:`rdna3-wmma-common-parameters`.

**Returns** ``v8int`` -- updated accumulator
(:math:`\text{srcA} \times \text{srcB} + \text{srcC}`).
