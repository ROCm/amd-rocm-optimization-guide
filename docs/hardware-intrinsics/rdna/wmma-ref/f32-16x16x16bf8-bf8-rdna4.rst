.. RST include fragment -- do not add to toctree.
   Intrinsic: __builtin_amdgcn_wmma_f32_16x16x16_bf8_bf8_w32_gfx12
   Include via: .. include:: wmma-ref/f32-16x16x16bf8-bf8-rdna4.rst

``__builtin_amdgcn_wmma_f32_16x16x16_bf8_bf8_w32_gfx12``
""""""""""""""""""""""""""""""""""""""""""""""""""""""""""

.. code-block:: cpp

   v8float __builtin_amdgcn_wmma_f32_16x16x16_bf8_bf8_w32_gfx12(
       v2int   srcA,
       v2int   srcB,
       v8float srcC);

Computes one step of a dense :math:`16 \times 16` FP32 accumulation with BF8
(E5M2) inputs for both :math:`\pmb{A}` and :math:`\pmb{B}`.  Eight BF8
values are packed into two 32-bit registers per lane (four 8-bit values per
register).

.. list-table::
   :header-rows: 1
   :widths: auto

   * - Parameter
     - Type
     - Description
   * - ``srcA``
     - v2int
     - Eight BF8 elements of :math:`\pmb{A}` per lane, packed into two
       32-bit registers (four 8-bit values per register).
   * - ``srcB``
     - v2int
     - Eight BF8 elements of :math:`\pmb{B}` per lane, packed into two
       32-bit registers.
   * - ``srcC``
     - v8float
     - Accumulator input: eight FP32 elements per lane.

**Returns** ``v8float`` -- updated accumulator
(:math:`\text{srcA} \times \text{srcB} + \text{srcC}`).
