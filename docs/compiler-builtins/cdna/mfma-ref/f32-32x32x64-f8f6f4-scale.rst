.. RST include fragment -- do not add to toctree.
   Intrinsic: __builtin_amdgcn_mfma_scale_f32_32x32x64_f8f6f4 (CDNA4 / gfx950)
   Include via: .. include:: mfma-ref/f32-32x32x64-f8f6f4-scale.rst

``__builtin_amdgcn_mfma_scale_f32_32x32x64_f8f6f4``
"""""""""""""""""""""""""""""""""""""""""""""""""""

Signature and parameters for this builtin.

.. code-block:: cpp

   v16float __builtin_amdgcn_mfma_scale_f32_32x32x64_f8f6f4(
       <vector>  srcA,
       <vector>  srcB,
       v16float  srcC,
       int       cbsz,
       int       blgp,
       int       op_sel_a,
       int       scale_a,
       int       op_sel_b,
       int       scale_b);

Computes one step of a :math:`32 \times 32` scaled sub-byte matrix
multiply-accumulate. Each instruction processes :math:`K=64` elements drawn
from a sub-byte floating-point format and accumulates into a
:math:`32 \times 32` FP32 tile. The :math:`\pmb{A}` and :math:`\pmb{B}`
input formats are selected independently at compile time by ``cbsz`` and
``blgp``, and each block of
output elements is scaled by a per-block scale factor loaded with
``v_mfma_ld_scale_b32``.

This is a **single-block** variant. The result is a ``v16float`` (16 FP32
elements per lane) using accVGPRs 0--15 in the :math:`32 \times 32` layout.

The vector type of ``srcA`` and ``srcB`` depends on the format specified by
``cbsz`` and ``blgp`` respectively:

.. list-table::
   :header-rows: 1
   :widths: auto

   * - Format code (``cbsz`` / ``blgp``)
     - Format
     - Vector type
     - Bits per element
   * - ``0``
     - FP8 (E4M3)
     - ``v8int`` (8 × ``int``)
     - 8
   * - ``1``
     - BF8 (E5M2)
     - ``v8int`` (8 × ``int``)
     - 8
   * - ``2``
     - FP6 (E2M3)
     - ``v6int`` (6 × ``int``)
     - 6
   * - ``3``
     - FP6 (E3M2)
     - ``v6int`` (6 × ``int``)
     - 6
   * - ``4``
     - FP4 (E2M1)
     - ``v4int`` (4 × ``int``)
     - 4

The ``srcA`` vector type must be consistent with the format selected by
``cbsz``, and the ``srcB`` vector type must be consistent with the format
selected by ``blgp``. Mixing a vector type with an incompatible format
code is undefined behavior.

.. list-table::
   :header-rows: 1
   :widths: auto

   * - Parameter
     - Type
     - Description
   * - ``srcA``
     - ``v8int`` / ``v6int`` / ``v4int``
     - :math:`\pmb{A}` matrix fragment per lane; element type and packing
       determined by ``cbsz``. Pass as ``int [[clang::ext_vector_type(N)]]``
       where :math:`N` is 8, 6, or 4.
   * - ``srcB``
     - ``v8int`` / ``v6int`` / ``v4int``
     - :math:`\pmb{B}` matrix fragment per lane; element type and packing
       determined by ``blgp``.
   * - ``srcC``
     - v16float
     - Accumulator input: 16 FP32 elements per lane.
   * - ``cbsz``
     - int (immediate)
     - Format selector for :math:`\pmb{A}`. Must be a compile-time constant in
       the range 0--4. See the format table above.
   * - ``blgp``
     - int (immediate)
     - Format selector for :math:`\pmb{B}`. Must be a compile-time constant in
       the range 0--4. See the format table above.
   * - ``op_sel_a``
     - int (immediate)
     - 2-bit selector that picks which quarter of ``scale_a`` applies to each
       output block. Must be a compile-time constant.
   * - ``scale_a``
     - int
     - Per-block scale register for :math:`\pmb{A}`. Applied to all
       :math:`\pmb{A}` input values before accumulation.
   * - ``op_sel_b``
     - int (immediate)
     - 2-bit selector that picks which quarter of ``scale_b`` applies to each
       output block. Must be a compile-time constant.
   * - ``scale_b``
     - int
     - Per-block scale register for :math:`\pmb{B}`. Applied to all
       :math:`\pmb{B}` input values before accumulation.

**Returns** ``v16float`` -- updated accumulator.

.. note::

   Unlike the standard ``cbsz`` and ``blgp`` modifiers described in
   :ref:`cdna-mfma-common-parameters`, in this builtin ``cbsz`` and ``blgp``
   are **format selectors**, not broadcast and lane-group-pattern modifiers.

.. note::

   This instruction uses round-toward-nearest-even semantics.
